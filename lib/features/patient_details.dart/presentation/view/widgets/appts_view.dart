import 'package:flutter/material.dart';

class ApptsView extends StatelessWidget {
  const ApptsView({super.key});

  @override
  Widget build(BuildContext context) {
    return 
       Center(child: Text('Appts'));  
       }
}