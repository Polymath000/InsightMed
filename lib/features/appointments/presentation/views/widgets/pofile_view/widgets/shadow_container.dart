import 'package:flutter/material.dart';

BoxShadow shadowContainer() => BoxShadow(
      color: Colors.grey.withValues(alpha: 0.3),
      spreadRadius: 5,
      blurRadius: 2,
      offset: Offset(0, 0),
    );